{
  "name": "@thenamespace/ui-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "storybook": "pnpm --filter storybook dev",
    "landing": "pnpm --filter landing dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "format:check": "turbo run format:check"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.6.2",
    "eslint": "^9.34.0"
  }
}
